<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="user.list">
      <field name="name">user list</field>
      <field name="model">user.user</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="user.action_window">
      <field name="name">user window</field>
      <field name="res_model">user.user</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="user.action_server">
      <field name="name">user server</field>
      <field name="model_id" ref="model_user_user"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="user" id="user.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="user.menu_1" parent="user.menu_root"/>
    <menuitem name="Menu 2" id="user.menu_2" parent="user.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="user.menu_1_list" parent="user.menu_1"
              action="user.action_window"/>
    <menuitem name="Server to list" id="user" parent="user.menu_2"
              action="user.action_server"/>
    -->
  </data>
</odoo>
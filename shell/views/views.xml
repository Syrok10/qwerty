<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="shell.list">
      <field name="name">shell list</field>
      <field name="model">shell.shell</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="shell.action_window">
      <field name="name">shell window</field>
      <field name="res_model">shell.shell</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="shell.action_server">
      <field name="name">shell server</field>
      <field name="model_id" ref="model_shell_shell"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="shell" id="shell.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="shell.menu_1" parent="shell.menu_root"/>
    <menuitem name="Menu 2" id="shell.menu_2" parent="shell.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="shell.menu_1_list" parent="shell.menu_1"
              action="shell.action_window"/>
    <menuitem name="Server to list" id="shell" parent="shell.menu_2"
              action="shell.action_server"/>
    -->
  </data>
</odoo>